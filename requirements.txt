# Versions figées : tout le monde installe exactement la même chose.
# Pour mettre à jour une dépendance, changer la version ici et tester —
# jamais d'installation à la main sans la répercuter dans ce fichier.

# Serveur web
Flask==3.1.3
Werkzeug==3.1.8
Flask-Cors==6.0.5

# Temps réel
Flask-SocketIO==5.6.1
simple-websocket==1.1.0

# Accès base de données
SQLAlchemy==2.0.54
pg8000==1.31.5

# Validation des entrées et documentation d'API (OpenAPI + Swagger UI)
pydantic==2.13.5
flask-pydantic-spec==0.8.7

# Authentification : jeton JWT en cookie, hachage des mots de passe, limitation de débit
flask-jwt-extended==4.7.4
argon2-cffi==25.1.0
flask-limiter==4.1.1

# Configuration
python-dotenv==1.2.2

# Tests
pytest==9.1.1

# Validation des entrées et documentation d'API ( OpenAPI + Swagger UI )
pydantic==2.13.5
flask-pydantic-spec==0.8.7
